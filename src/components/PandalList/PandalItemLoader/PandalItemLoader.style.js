import { StyleSheet } from "react-native";
import { PRIMARY_SHADE_COLOR } from "../../../assets/styles/color";

const styles = StyleSheet.create({
  title: {
    backgroundColor: PRIMARY_SHADE_COLOR
  }
});

export default styles;
