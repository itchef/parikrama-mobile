import { StyleSheet } from "react-native";
import { SECONDARY_COLOR } from "../../assets/styles/color";

const styles = StyleSheet.create({
  toastWrapper: {
    backgroundColor: SECONDARY_COLOR
  }
});

export default styles;
