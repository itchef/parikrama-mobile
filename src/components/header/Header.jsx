import React from "react";
import { AppBar } from "@material-ui/core";

const Header = () => (
  <AppBar className="header__wrapper" position="fixed" color="primary">
    <span>Parikrama</span>
  </AppBar>
);

export default Header;
